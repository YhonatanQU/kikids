export function Footer() {
  return (
    <footer className="mt-12 border-t border-ink-100 bg-white px-4 py-8 text-center md:px-8">
      <p className="text-sm font-semibold text-ink-700">KIKIDS</p>
      <p className="mt-1 text-xs text-ink-400">© {new Date().getFullYear()} KIKIDS — Moda infantil. Perú.</p>
    </footer>
  );
}
